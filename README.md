Solution for https://hackattic.com/challenges/backup_restore

### Prerequisites 
* Install docker https://docs.docker.com/engine/install/
* Install pip requirements `pip install -r requirements.txt`
* Run `python main.py {token}`